#!/bin/bash

#get all devices from database
#for each device in devices {
#	if (device.experation_date < current_time) {
#		removeObject.push(device);
#	}
#}

#for each device in removeObject {
#	DELETE * FROM database WHERE ID = device.id
#	parse public key from device.config
#	sudo wg set wg0 peer public_key remove
#}

#This script runs every day at 00:05